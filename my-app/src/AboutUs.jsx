import { Container, Typography } from '@mui/material';

const AboutUs = () => (
  <Container>
    <Typography variant="h4">About Us</Typography>
    <Typography variant="body1">This is the about us section.</Typography>
  </Container>
);

export default AboutUs;
